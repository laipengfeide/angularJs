spring:
  application:
    name: gateway
  cloud:
    config:
      uri: http://25.30.10.66:8888
      fail-fast: true
      username: user
      password: password
      retry:
        initial-interval: 2000
        max-interval: 10000
        multiplier: 2
        max-attempts: 10
